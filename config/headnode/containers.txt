# NGC Container List for FSx Lustre
# Format: docker://registry/image:tag
# Lines starting with # are comments
#
# These containers will be downloaded to /fsx/containers/ for shared access
# across all compute nodes

# PyTorch (recommended for most LLM workloads)
# Includes: PyTorch, CUDA, cuDNN, NCCL, Apex, Transformer Engine
docker://nvcr.io/nvidia/pytorch:24.01-py3

# NeMo Framework (NVIDIA's LLM training framework)
# Includes: NeMo, Megatron-LM, PyTorch, NCCL optimizations
docker://nvcr.io/nvidia/nemo:24.01

# TensorFlow (if needed)
# docker://nvcr.io/nvidia/tensorflow:24.01-tf2-py3

# JAX (if needed)
# docker://nvcr.io/nvidia/jax:24.01-py3

# Triton Inference Server (for serving)
# docker://nvcr.io/nvidia/tritonserver:24.01-py3
